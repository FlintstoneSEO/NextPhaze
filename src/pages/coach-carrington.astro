---
import { getEntry } from 'astro:content';
import PageBuilderPage from '@/components/PageBuilderPage.astro';
const page = await getEntry('pages', 'coach-carrington');
if (!page) throw new Error('Missing coach content.');
---
<PageBuilderPage page={page} />
