label: Coach hero
icon: dashboard
picker_preview: { text: [Coach hero], icon: [dashboard] }
preview: { text: [{ key: title }, Coach hero], icon: [dashboard] }
value:
  _type: coach_hero
  eyebrow:
  title:
  copy:
  button_label: Book Training
  button_href: /book-training/
  image:
  image_alt:
_inputs:
  eyebrow: { type: text }
  title: { type: text }
  copy: { type: textarea }
  button_label: { type: text }
  button_href: { type: text }
  image: { type: image }
  image_alt: { type: text }
